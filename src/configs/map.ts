export const TOKEN_SIZE = 48; // Size of the token in pixels
export const MAP_WIDTH = 5000; // Map height in pixels
export const MAP_HEIGHT = 5000; // Map width in pixels
export const HEADER_HEIGHT = 56; // Header height in pixels
export const MOVEMENT_SPEED = 500; // Movement speed in pixels per second
export const DEFAULT_POSITION = { x: MAP_WIDTH / 2, y: MAP_HEIGHT / 2 }; // Default position of the player