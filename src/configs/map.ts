export const TOKEN_SIZE = 48; // Size of the token
export const MAP_WIDTH = 2800; // Map height
export const MAP_HEIGHT = 1800; // Map width
export const HEADER_HEIGHT = 56; // Header height
export const MOVEMENT_SPEED = 250; // Movement speed per second
export const DEFAULT_POSITION = {
    x: MAP_WIDTH / 2,
    y: MAP_HEIGHT / 2,
    width: TOKEN_SIZE,
    height: TOKEN_SIZE,
}; // Default position of the player on render
